return {
  {
    "mg979/vim-visual-multi",
    branch = "master",
    -- init = function()
    --   -- vim-visual-multi のグローバル設定はプラグインロード前に行う必要があるため
    --   -- config ではなく init の中に記述します。
      
    --   -- デフォルトのキーマップを明示（変更したい場合はここの文字列を変更します）
    --   vim.g.VM_maps = {
    --     ["Find Under"]         = "<C-n>",
    --     ["Find Subword Under"] = "<C-n>",
    --   }
    -- end,
  }
}
